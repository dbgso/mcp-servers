// File contents intentionally empty.
// The historical Drizzle-chain fake was retired when ops moved to the
// engine-agnostic DataSource abstraction (`createFakeDataSource` in
// ./fake-data-source.ts). Adapter-level fakes now live in the per-engine
// packages (`mcp-shared-db-sql`, `mcp-shared-db-postgres`).
export {};

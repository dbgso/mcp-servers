// Engine-specific test fixtures live in the adapter packages
// (`mcp-shared-db-sql`, `mcp-shared-db-postgres`). The operation layer in
// this package is engine-agnostic, so no schema fixture is needed here.
export {};

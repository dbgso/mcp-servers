// Engine-specific helpers (table/column resolution, projection building) live
// in adapter packages such as `mcp-shared-db-postgres` /
// `mcp-shared-db-sql`. This file is intentionally empty — the operation layer
// is engine-agnostic and no longer needs them.
export {};

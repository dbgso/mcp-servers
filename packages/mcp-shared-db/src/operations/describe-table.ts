// Re-exported from mcp-shared-db-core. The core op uses DatabaseCoreContext,
// which DatabaseOperationContext extends, so it works in the RDB registry too.
export { describeTableOp } from "mcp-shared-db-core";

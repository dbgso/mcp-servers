/**
 * Adds two numbers together.
 *
 * @param a - the first number
 * @param b - the second number
 * @returns their sum
 * @example add(1, 2)
 */
export function documented(a: number, b: number): number {
  return a + b;
}

export const undocumented = documented;
